'use strict'
const Form = require('./Form')
const Input = require('./Input')
const Swiper = require('./Swiper')

var Forms = {
    Form: Form,
    Input: Input,
    Swiper: Swiper,
}
module.exports = Forms
